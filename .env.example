# Markdown Decision Support — required environment keys.
#
# This is a static, client-side single-page app: no backend, no API keys,
# no runtime secrets. The deploy workflow injects nothing. This file exists
# to satisfy the constitution's no-drift gate; add a key here the moment the
# deploy starts injecting one.
